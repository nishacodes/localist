<%= link_to '<i class="fa fa-trash-o"></i>'.html_safe,  list_place_path(:list_id => list.id, :id => place.id), method: :delete, data: { confirm: 'Are you sure?'},:class => 'pull-right', remote: true  %>
<%= link_to '<i class="fa fa-pencil"></i>'.html_safe, edit_list_place_path(:list_id => list.id, :id => place.id), :class => 'pull-right'  %>
