$('#edit_list_place_<%= @place.id %>').hide().remove();
bindAjax();

function bindAjax() {
  var $place = $('#edit_list_place_<%= @place.id %>').parent();
  console.log($place);
  // $forms.on("ajax:success", function(e, data, status, xhr) {
  //   $(this).parent().remove();
  // }).bind("ajax:error", function(e, xhr, status, error) {
  //   $(this).append("<h3>" + error + "</h3>");
  // });
}