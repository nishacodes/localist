<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @list.name %>
</p>

<p>
  <b>User:</b>
  <%= @list.user_id %>
</p>


<p>
  <%= link_to 'Add a place', new_list_place_path(@list) %> <br>
  <b>Places</b>
  <% @places.each do |place| %>
    <li><%= place.name %></li>
  <% end %>
</p>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
